# 开发进度检查点

## 当前目标

里程碑 1：成都血战到底完整 AI 对局与可玩 UI 节点已完成。当前准确起点是逐事件本地保存/恢复、倒计时与真实浏览器内测。

需求基线见 `PRD.md`。

## 已完成

### 1. 产品需求

- 已完成并冻结 MVP PRD。
- 已确认成都主流血战到底规则、番型、计分、定缺、碰杠胡、血战退出、终局结算和里程碑拆分。

### 2. 规则基础层

位置：`src/game/`

- `rules.ts`：里程碑 1 规则常量和版本。
- `types.ts`：唯一牌实例、玩家、牌局、事件、响应窗口、合法动作等领域类型。
- `core.ts`：108 张牌、确定性随机、洗牌、开局、排序、定缺推荐和超时出牌。
- `scoring.ts`：标准胡牌、副露胡牌、七对升级、基础番型、番数封顶和基础计分。
- `ai.ts`：仅消费合法动作与公开视图的确定性三风格 AI、单步推进器和整局模拟器。

已支持：

- 108 张万条筒牌，每张牌具有唯一 ID；
- 相同 seed 得到相同庄家、发牌和牌墙；
- 随机庄家，庄家 14 张、其他玩家 13 张，牌墙剩余 55 张；
- 进攻型、稳健型、效率型三个 AI 风格分配；
- AI 视图仅包含自身手牌、公开弃牌/鸣牌/定缺及牌墙余量，不暴露对手手牌和牌墙顺序；
- 效率型偏连接牌效，进攻型偏碰杠、刻子对子与花色集中，稳健型结合公开信息避险；
- AI 合法胡必胡，按推荐定缺并遵守强制定缺，可推进 AI 定缺、回合与响应；
- 四席 AI 可按固定 seed 完整模拟，保持事件序号、108 实体唯一/守恒及分数守恒；
- 标准胡、碰碰胡、清一色、七对、龙七对、双龙七对、清七对、金钩钓；
- 5 番封顶，底分乘以 2 的番数次方；
- 强制定缺、结构牌效定缺推荐和合法超时出牌。

### 3. 事件驱动状态机

- `engine.ts`：统一合法动作、命令执行和事件追加。
- `engine.test.ts`：状态机固定牌例测试。

已支持：

- 四家定缺完成后进入庄家出牌；
- 摸牌、出牌和跳过已胡玩家轮转；
- 碰、明杠、补杠、暗杠、胡、过；
- 胡优先于杠、杠优先于碰；
- 一炮多响；
- 仅补杠可被抢杠胡；
- 自摸、杠上花、杠上炮、抢杠胡、海底加番上下文；
- 点杠 2 分、补杠每家 1 分、暗杠每家 2 分；
- 杠上炮呼叫转移；
- 过手胡限制及自己摸牌后解除；
- 胡牌玩家退出后续行动和支付；
- 三家胡牌或牌墙耗尽结束；
- 相同初始状态和命令序列得到相同结果；
- 终局仅处理未胡活跃玩家，严格按未听退税、查花猪、查叫执行；
- 未听枚举合法候选牌并计算最高叫，花猪固定未听，最高叫受 5 番封顶；
- 未听逐笔退还原支付关系的杠分，不退其他分数或重复退款；
- 花猪按 32 分赔付活跃非花猪，查叫允许花猪支付但禁止其收益；
- 终局转账保留原因和来源事件，事件顺序可验证、分数守恒且重复结束幂等。

## 当前验证状态

最近一次验证：

- `npm test -- --run`：通过，10 个测试文件、64 个测试全部通过；
- `npm run build`：通过；
- `npx eslint src/game src/components/SichuanGame.tsx src/App.tsx`：通过；
- 事件回放、状态不变量与存档恢复测试均已纳入全量验证；
- 麻将牌已替换为现代川麻简洁牌面，红万、绿条、蓝筒，并移除运行时代码中的旧 SVG 牌面依赖；
- 定缺前先完整展示用户起手牌，用户确认前 AI 不定缺；牌河与鸣牌复用同一套现代牌面；
- 终局新增独立查叫关系区，明确未听付款方、听牌收款方与赔付金额；
- 全仓 `npm run lint` 仍受原项目 `src/utils/tracker.ts` 的既有格式问题影响，与本次代码无关；
- 新牌面移除旧 SVG 组件打包后，主 JS 产物降至约 442 kB，Vite 大包体积警告已消除。

## 尚未完成

### 规则与结算

- 已完成命令级稳定检查点回放 API、损坏检查点拒绝及状态不变量测试；
- 已完成存档加载时的稳定事件序号和完整状态不变量校验；
- 已完成终局关键事件时间线，并支持切换查看完整事件流水。

### AI

- AI 决策与单步推进已完成；页面已接入 350–650ms 拟人停顿，并在 StrictMode 下通过 timeout cleanup、调度 token 与最新状态引用避免重复行动。
- UI 可玩节点已完成：用户定缺、单击选牌/双击出牌、出牌与响应按钮、碰杠胡过、四方牌桌信息展示和结算面板均已接通；下一步为本地存档/恢复。

### 页面

已完成：

- 新版首页与醒目的“四人实战”主入口，旧训练能力保留在“专项训练”；
- 用户定缺与推荐标记，AI 依次定缺；
- 深色四方牌桌、四家独立牌河、公开鸣牌、分数和行动状态；
- 用户首次单击选牌、再次单击同牌确认出牌、触屏出牌按钮及全部合法动作投影；
- AI 暗牌全程保护（仅展示数量/牌背），胡牌后仅公开胡牌状态与番数；
- 终局排名、战绩、即时/终局计分流水结算页；
- 桌面与手机竖屏响应式布局。

已完成：

- 正式智能复盘：终局使用 `analyzeGame` 输出 2 个主要问题、1 个优秀决策和机会数趋势；
- 每条结论支持“认可 / 不认可”，不认可原因与评估版本写入本地反馈记录；
- 近三局历史摘要改用朱扬理论报告，不再依赖旧的 AI 对拍结果。

### 保存与恢复

- 逐事件本地保存（用户、AI、超时动作成功后保存）；
- 首页继续牌局，仅保留当前未完成局；
- 页面离开时尽可能保存并暂停调度；
- 恢复到稳定决策点，当前操作阶段重新获得完整时限；
- 放弃牌局二次确认、删除存档且不计统计；
- 用户出牌 15 秒、响应 8 秒倒计时，超时执行合法自动动作。

## 下一步准确起点

1. 移动端实机内测，继续修正不同屏幕上的牌桌密度与触控细节（390px 定缺面板横向溢出已修复）。
2. 在真实浏览器中验收终局关键/完整事件切换与长列表滚动。
3. 准备上传 GitHub 开发分支。

## Git 状态

当前分支：`main`，跟踪 `origin/main`。

**2026-08-20 已建立阶段性基线**：Trae 停用，工作交接给 Codex。全部改动（含 `PRD.md`、`DEVELOPMENT_PROGRESS.md`、`src/game/`、`src/knowledge/`、`src/review/`、`docs/`）已提交并打 tag，详见 `git log`。完整交接说明见 `docs/HANDOVER.md`。

验证基线（实测）：92 测试全绿、`npm run build` 通过、核心领地 ESLint 0 错误。
