# 四川麻将实战教练 PRD（MVP）

## 1. 文档信息

- 产品形态：响应式 Web 应用
- 目标版本：MVP
- 基础项目：现有四川麻将基本功训练应用
- 核心玩法：成都主流“血战到底”
- 核心目标：让用户通过实战发现长期决策问题，并通过专项训练和再次实战完成改善闭环
- 商业模式：MVP 免费，不设置付费功能

## 2. 产品背景

现有产品以基本功训练为主，能够训练听牌、牌型和出牌等单项能力，但存在两个问题：

1. 用户容易认为重复训练枯燥，缺少持续使用动力；
2. 用户通常不知道自己的真实问题，难以主动选择合适的训练内容。

新版产品以完整实战为起点。系统记录用户在真实牌局语境中的决策，通过算法识别反复出现的问题，提供局后复盘与动态专项题，最后通过再次实战验证改善效果。

## 3. 产品定位

### 3.1 一句话定位

面向中高阶四川麻将玩家的智能实战教练，通过 AI 对局、算法复盘和针对性训练，帮助用户发现并改善长期决策习惯。

### 3.2 目标用户

核心用户为中高阶四川麻将玩家：

- 已熟悉四川麻将基本规则；
- 能独立完成牌局，但表现不稳定；
- 希望提高牌效、攻防和鸣牌决策；
- 难以仅凭输赢发现自己的决策问题；
- 不愿长期进行与自身问题无关的枯燥训练。

### 3.3 核心价值

- 用完整对局暴露真实问题；
- 用可追溯、可解释的量化方法分析问题，而非只凭结果评价；
- 每局聚焦 2 个主要问题和 1 个优秀决策，降低复盘负担；
- 根据问题自动生成训练，不要求用户自己找课程；
- 用后续实战验证训练是否转化为实际能力。

## 4. 产品目标与非目标

### 4.1 最终 MVP 目标

最终 MVP 打通“手机号验证码登录 → 单局实战 → 复盘 → 动态专项训练 → 再战验证”的完整闭环，并具备：

1. 符合本 PRD 规则口径的完整血战到底对局；
2. 对牌效、攻防、鸣牌三类决策的记录和评估；
3. 每局 2 个主要问题和 1 个优秀决策的复盘；
4. 将关键问题转化为合法、相似的动态专项题；
5. 长期问题、能力变化和训练效果记录；
6. 账户、跨设备同步及中途退出续局。

最终 MVP 是完整产品验收目标，不等同于首个开发里程碑。各能力按第 12 章分阶段交付，优先验证规则与诊断认可度，避免要求首期同时完成精确期望值引擎、正式账户和完整训练闭环。

### 4.2 MVP 核心成功指标

核心指标：**闭环完成率**。

定义：开始一局实战的用户中，在规定统计周期内依次完成以下行为的比例：

1. 完成一局实战；
2. 查看复盘总结；
3. 完成至少一组推荐专项训练；
4. 开始一局验证实战。

同时观察：

- 单局完成率与单局时长；
- 复盘查看率与复盘耗时；
- 主要问题和优秀决策查看率；
- 专项训练开始率与完成率；
- 验证实战开始率；
- 复盘建议认可度及复核率；
- 同类问题的平均决策损失和错误率变化。

体验目标：单局 8–12 分钟，单局复盘 3 分钟内完成。

### 4.3 MVP 非目标

- 多种麻将规则同时上线；
- 真人实时匹配；
- 牌谱导入；
- 局中实时指导；
- 可视化牌局回放；
- 完整社交、排行榜或赛事系统；
- 纯大模型生成决策结论；
- 以段位和奖励为核心的重度游戏化体系；
- 付费及商业化能力。

## 5. 规则范围

### 5.1 规则总则

MVP 采用成都主流“血战到底”：

- 四人牌局，用户对战三个 AI；
- 使用 108 张牌，仅包含万、条、筒 1–9 各四张，不含字牌和花牌；
- 开局不做配置，点击“开始实战”后直接随机确定庄家并随机发牌；庄家 14 张先出牌，其他三家各 13 张；
- 四家均从 0 分开始记录本局净得分，允许出现负分；
- 一名玩家胡牌后，其他未胡玩家继续；
- 启用定缺、呼叫转移、查叫、查花猪；
- 不启用换三张；
- 固定底分为 1 分；
- 胡牌基础计算为：`底分 × 2^总番数`；
- 总番数 5 番封顶；番型和特殊情形可以叠加，但叠加后的计分番数仍不超过 5 番。

对局引擎、AI、复盘、训练和验收测试必须共用同一版本化规则定义。

### 5.2 定缺与出牌约束

- 发牌后系统综合花色张数、孤张结构和有效搭子，向用户推荐预期牌效损失最小的缺门；用户可在万、条、筒中修改并确认；
- 开局完成定缺后，持续向四家展示定缺状态；
- 玩家手中仍有缺门牌时，必须优先打出缺门牌；
- 玩家手中仍有缺门牌时不能胡牌；
- 合法动作生成、AI 行动和复盘评估均必须遵守该强制约束。

### 5.3 番型与番数

#### 基础番型

| 番型 | 番数 |
| --- | ---: |
| 平胡 | 0 番 |
| 碰碰胡 | 1 番 |
| 清一色 | 2 番 |
| 七对 | 2 番 |
| 金钩钓（又称大单吊，为同一术语） | 2 番 |

#### 七对升级

| 番型 | 番数 |
| --- | ---: |
| 龙七对 | 3 番 |
| 清七对 | 4 番 |
| 双龙七对 | 4 番 |

#### 特殊情形

以下情形各加 1 番，可以互相及与番型叠加，但总计受 5 番封顶：

- 自摸：采用加番规则；
- 杠上花；
- 杠上炮；
- 抢杠胡；
- 海底捞：牌墙最后一张可摸牌自摸；
- 海底炮：牌墙最后一张可摸牌被打出后点炮。

### 5.4 胡牌响应与操作优先级

- 支持一炮多响；
- 同一弃牌被多人胡时，多人胡同时成立，点炮者分别向每位胡牌者支付对应胡分；
- 同一张牌触发响应机会时，一次展示该玩家全部合法的碰、杠、胡、过按钮；不支持吃牌；
- 响应时限为 8 秒，超时默认过；
- 同一张牌触发多个操作时，优先级为：胡 > 杠 > 碰；
- 多人胡之间不互相排斥，不因座次取消其他合法胡牌；
- 碰牌成功后，碰牌者进入正常的 15 秒出牌阶段。

### 5.5 杠分与呼叫转移

杠分即时结算，底分固定为 1 分：

- 同时存在多个合法杠项时，界面逐项列出具体牌及明杠、补杠或暗杠类型，由玩家选择；
- 抢杠胡只允许针对补杠；补杠被抢后不成立，且不结算该次杠分；
- 点杠：由点杠者向杠牌者支付 2 倍底分；
- 补杠：每位尚未胡牌的玩家向杠牌者支付 1 倍底分；
- 暗杠：每位尚未胡牌的玩家向杠牌者支付 2 倍底分。

杠上炮与呼叫转移：

- 玩家杠牌后立即出牌并造成点炮时，该胡牌计“杠上炮”并加 1 番；
- 同时触发呼叫转移：将该玩家本次杠牌刚获得的完整杠分转移给胡牌者；
- 一炮多响时，每位胡牌者均获得一份完整的本次杠分转移，不在多位胡牌者之间拆分。

### 5.6 过手胡

- 玩家放弃某张当时可以胡的牌后，本轮内不能胡收益相同或更低的牌；
- 玩家之后自己摸牌时，过手胡限制解除；
- 是否受限按该决策时点依规则计算的实际收益比较，而非只比较牌面名称。

### 5.7 牌局结束条件

以下任一条件先满足，牌局立即进入终局结算：

1. 已有三家胡牌，仅剩一名未胡玩家；
2. 牌墙摸完。

### 5.8 终局结算顺序

终局严格按以下顺序处理：

1. **未听退税**：未听牌玩家将此前所得杠分退还给原支付者；
2. **查花猪**：花猪向所有非花猪玩家分别赔付按 5 番封顶计算的分数；
3. **查叫**：未听牌玩家向已听牌玩家赔付其最高叫，按可胡最高番计算且受 5 番封顶。

花猪不作为查叫收益方参与查叫，不获得查叫赔付。所有终局款项必须保留支付者、收款者、原因和金额明细，以支持结算解释和规则测试。

### 5.9 实战介入原则

- 实战期间不提示决策优劣；
- 不展示推荐动作、胜率或风险；
- 不因用户做出错误选择而打断牌局；
- 所有分析统一在牌局结束后计算和呈现，避免破坏真实决策环境。

## 6. 核心用户流程

### 6.1 首次用户

1. 使用手机号验证码注册或登录；
2. 通过极短说明了解“先实战、后诊断”，直接开始首战；
3. 完成动态难度 AI 对局；
4. 局后等待复盘计算并查看进度；
5. 查看 2 个主要问题和 1 个优秀决策；
6. 进入系统生成的专项训练，逐题获得即时反馈；
7. 完成训练后开始验证实战；
8. 在积累足够同类样本后查看是否改善。

### 6.2 持续用户

1. 登录后进入首页，核心入口为“今日教练建议”；
2. 查看当前最突出问题、近期变化和建议行动；
3. 继续上次牌局、开始专项训练或开始实战；
4. 每完成一局，更新问题清单、能力雷达和成长曲线；
5. 系统每 3 局重新评估 AI 难度及训练内容。

### 6.3 中途退出与续局

- 里程碑 1 仅使用本地存档，并且只保留当前一局未完成牌局；正式 MVP 再支持云端同步；
- 每个完整牌局事件结束后立即保存；切到后台、关闭页面或打开暂停菜单时停止倒计时；
- 用户返回首页后，由用户主动点击“继续牌局”，从最近一个稳定决策点恢复，不重新发牌或改变随机结果；恢复后，当前操作阶段倒计时重置为该阶段的完整时限；
- 放弃牌局必须二次确认；确认后删除存档，且该局不进入能力或问题统计。

## 7. 信息架构

最终 MVP 一级模块：

1. **首页**：新版以实战为主入口；里程碑 1 提供“开始实战”，存在存档时同时提供“继续牌局”；后续里程碑再承载“今日教练建议”和继续闭环；
2. **实战**：完整血战到底 AI 对局；
3. **复盘**：整局总结、2 个主要问题、1 个优秀决策；
4. **专项训练**：根据个人问题生成动态题组；
5. **成长**：问题清单、能力雷达、成长曲线；
6. **账户**：手机号验证码登录、退出及进度同步。

原有听牌、牌型、出牌等训练统一收进“专项训练”，不再作为首页或一级导航中的孤立主功能；动态生成与按实战诊断自动调用在后续里程碑实现。

## 8. 功能需求

### 8.1 账户与数据同步

#### 需求

- 正式 MVP 使用手机号验证码登录；
- 新用户登录后仅展示极短说明，随后直接开始首战；
- 保存牌局记录、用户决策、复盘结果、训练结果和成长数据；
- 支持跨设备恢复进度；
- 中途退出自动保存，能够从同一决策点续局；
- 未完成流程时，首页提供“继续牌局”或“继续上次进度”。

#### 验收标准

- 用户在设备 A 完成牌局后，可在设备 B 查看该局复盘；
- 刷新或重新登录后，闭环进度不丢失；
- 中途退出后恢复到同一决策点，局面和随机结果不变；
- 不同用户的数据严格隔离。

### 8.2 AI 实战

#### 基础需求

- 用户与三个 AI 完成一局血战到底；
- 支持摸牌、出牌、碰、杠、胡、过和定缺等必要操作，禁止吃牌；
- 胡牌者公开完整手牌和牌型并留在原座位区域；此后退出摸牌、出牌和响应，不再参与后续自摸、杠分、查叫或花猪支付，但保留其分数和已经结算的结果；
- AI 必须遵守相同规则和信息边界，不读取不可见牌；
- 完整记录每个决策时点的当时可见信息、合法动作、用户动作和后续事件；
- 页面适配手机与桌面浏览器；
- 局中不展示复盘或推荐提示。

#### AI 风格与拟人表现

三个 AI 风格分别为：

- 进攻型；
- 稳健型；
- 效率型。

AI 普通操作前加入约 0.5–1 秒的拟人短停顿。用户或 AI 胡牌时，短暂停顿约 1–2 秒，展示胡牌与分数变化后再继续。停顿不得明显拖慢单局 8–12 分钟的体验目标。

#### 动态难度

- 初次用户以中等基准强度开始；
- 明确告知用户当前启用动态难度，并强调系统不读取任何隐藏信息；
- 每 3 局重新评估一次难度，不因单局结果立即大幅变化；
- 主要依据能力评分和决策损失调整；
- 胜负、净得分和用户偏好仅用于校准，不作为主要依据；
- 最低难度仍保持基本牌效，不通过故意送炮或非法弱化制造胜利。

#### 验收标准

- 用户可完整打完一局，常规单局时长目标为 8–12 分钟；
- 所有动作和结算符合第 5 章规则；
- 牌局可由完整事件记录确定性还原，MVP 暂不要求可视化回放；
- AI 不做非法动作，不使用隐藏信息；
- 三种风格在遵守基本牌效的前提下体现可识别差异；
- 动态难度调整时机及依据符合上述口径。

### 8.3 牌桌交互与视觉

#### 布局与信息

- 使用四方牌桌，用户固定在下方，三个 AI 分别位于左、上、右；
- 每家在自己面前拥有独立牌河，弃牌按先后顺序排列，并高亮最后一张弃牌；
- 手牌始终按万、条、筒及数字自动排序；新摸到的牌暂放最右侧，待完成本次操作后再并入排序；
- 持续展示四家定缺状态；
- 持续展示四家鸣牌区和实时分数；
- 持续展示牌墙剩余数量及当前行动者；
- 正式验收覆盖手机竖屏和桌面宽屏，关键信息在两类尺寸下均清晰可读。

#### 出牌与响应交互

- 采用两次点击确认出牌：首次点击选中牌，再次点击同一张牌确认打出；
- 第一次选牌后必须有明确选中态；
- 选择另一张牌时只切换选中项，不直接出牌；
- 非当前行动者或不合法牌不可确认打出；
- 每次出牌限时 15 秒；超时优先摸切；若摸到的牌受定缺限制而不能打出，则在合法缺门牌中选择牌效损失最小的牌，并尽量不破坏对子和搭子；
- 里程碑 1 仅实现超时出牌，不实现自动胡或整局托管；
- 出现响应机会时展示全部合法的碰、杠、胡、过操作；响应倒计时为 8 秒，超时默认过。

#### 视觉方向

专业竞技风格，使用深色视觉、克制动效和清晰的数据层级；动效服务于行动反馈，不遮挡牌面、牌河、分数或结算信息。

### 8.4 决策评估

#### 评估对象

1. **牌效选择**：出牌对向听数、有效进张、成牌机会和潜在收益的影响；
2. **攻防判断**：继续进攻、转守、防守选牌及危险牌处理；
3. **鸣牌策略**：碰、杠、过对速度、收益和风险的影响。

#### 最终评估目标

产品最终优化目标是：**只使用决策当时可见信息，使长期期望得分最大化**。

- 在每个决策点比较合法候选动作；
- 最终以最佳动作与用户动作的长期期望得分差定义决策损失；
- 不使用后来出现的隐藏牌或最终输赢反向判定当时选择；
- 攻防判断基于桌面可见牌、出牌顺序、四家定缺和鸣牌信息推断对手威胁；
- 相同局面、参数和评估版本下结果应可复现。

#### 首版实现口径

基础复盘阶段先用可解释启发式近似最终目标，包括牌效、危险度和局势规则等因素：

- 首版输出的是启发式综合评估或近似决策损失；
- 不得将其宣传或标注为“精确期望值”；
- 先通过用户反馈和专家判断验证诊断认可度；
- 后续再升级为混合评估和模拟期望值，逐步接近长期期望得分最大化目标；
- 历史结果必须记录算法版本和口径，避免升级后混用。

#### 输出

每个被选入复盘的决策点输出：

- 用户动作和推荐动作；
- 主要候选动作排序；
- 当前版本可支持的量化差异；
- 问题类型与严重程度；
- 简短、可解释的关键原因；
- 明确标注评估方法及是否属于近似值。

### 8.5 局后复盘

#### 计算与整局总结

- 复盘在牌局结束后计算，不占用局中决策时间；
- 计算期间展示明确进度；
- 复盘体验目标为 3 分钟内完成；
- 首屏展示本局综合表现、牌效/攻防/鸣牌评分、与近期水平对比，以及进入重点复盘的入口；
- 评分反映决策质量，不以名次或净得分直接代替。

#### 2 个主要问题 + 1 个优秀决策

每局默认展示恰好 3 个重点决策：

1. **2 个主要问题**：优先选择损失较高、能代表重复问题、用户可理解且可训练的决策，两个问题尽量避免完全重复；
2. **1 个优秀决策**：选择当时具有代表性、用户处理合理且有明确学习价值的决策。

每个重点决策展示：

- 决策发生时的完整可见局面；
- 用户当时的选择；
- 推荐选择或优秀之处；
- 当前评估版本支持的量化差异与关键数据；
- 简短明确的原因；
- 对主要问题提供进入对应专项题的入口。

#### 用户反馈与复核

- 用户可对复盘结论提交“认可”或“不认可”；
- 选择“不认可”时可填写原因；
- “不认可”反馈将该结论标记为待复核，但不即时改变原结论、评分或推荐；
- 反馈用于后续人工或算法验证诊断认可度。

#### 表达风格

- 先给结论，再展示关键数字，最后简短解释；
- 不使用冗长报告；
- 不用绝对化语言掩盖概率判断；
- 明确区分“错误”“明显次优”“可接受选择”和“优秀决策”；
- 首版近似评估不得伪装成精确期望值。

### 8.6 个人问题画像

#### 长期问题成立条件

同类问题同时满足以下条件后，才成立为长期问题：

1. 至少出现 3 次；
2. 累计决策损失达到阈值。

累计决策损失阈值需通过评估验证确定，当前不预设数值。首版使用近似决策损失时，界面和数据定义应明确其评估口径。

#### 问题优先级

问题优先级综合以下因素计算：

- 出现频率；
- 累计决策损失；
- 近期程度。

不得仅按单次最大损失或最近一次结果排序。

#### 问题记录

- 问题名称和解释；
- 所属能力维度；
- 出现次数与频率；
- 累计决策损失；
- 最近出现时间；
- 当前状态：新发现、训练中、待验证、改善中、已改善、再次出现；
- 关联牌局、重点决策和专项训练；
- 评估引擎版本。

### 8.7 动态专项训练

#### 需求

- 根据主要问题和问题画像生成相似但不相同的决策题；
- 使用模板参数化方式生成合法的相似题，不直接复制原牌面；
- 每组训练只聚焦一个明确问题；
- 保留必要牌局语境，不退化为纯牌型题；
- 用户每完成一题，立即展示结果、关键数据和解释；
- 根据连续作答表现动态调整题目难度；
- 训练达标后将问题状态改为“待验证”。

#### 题组建议结构

- 每组 5 题；
- 前 3 题覆盖典型情形；
- 后 2 题用于变式和迁移；
- 训练达标的具体正确率或决策损失阈值待验证，不在当前阶段自行设定；
- 未达标时补充同类型变式题，而非简单重复原题。

现有听牌、牌型、出牌等基本功能力可作为动态题目的底层组件复用，但入口和组织方式改为由诊断问题驱动。

### 8.8 再战验证

#### 需求与状态

- 完成专项训练后，首页突出展示“开始验证实战”；
- 验证实战仍为正常完整牌局，不人为保证出现指定局面；
- 训练后至少积累 5 个同类实战决策，才能判断是否已改善；
- “已改善”要求训练后的平均决策损失和错误率都较训练前明显下降；
- “明显下降”的具体比例待验证，不自行预设；
- 无足够相关样本时保持“待验证”或“改善中”，不得宣称已改善；
- 已改善问题再次超过经验证的判定阈值时，更新为“再次出现”。

### 8.9 首页与成长

#### 首页入口

- 新版首页以实战为主入口，提供“开始实战”；点击后不经过配置，直接进入随机发牌流程；
- 存在未完成牌局本地存档时，同时提供“继续牌局”，且恢复动作必须由用户主动触发；
- 原有训练入口统一归入“专项训练”。

#### 今日教练建议

- “今日教练建议”在后续里程碑实现，正式 MVP 中作为老用户首页的教练入口；
- 建议基于当前最高优先级问题和闭环状态，明确推荐继续牌局、专项训练或验证实战；
- 存在中断牌局时，同时突出“继续牌局”。

#### 能力雷达

- 展示牌效、攻防、鸣牌三个维度；
- 每项采用 0–100 量化分数；
- 同时展示样本量和可信度；
- 样本不足时明确显示“数据积累中”，不得用高精度外观制造确定性；
- 分数基于近期有效决策，不因单局运气大幅变化。

#### 问题清单与成长曲线

- 按出现频率、累计决策损失和近期程度形成的综合优先级展示问题；
- 展示问题状态、证据和推荐下一步行动；
- 成长曲线展示三项能力分、平均决策损失、错误率、训练完成情况及问题状态节点；
- 能力变化不直接等同于输赢或净得分变化。

### 8.10 里程碑 1 结算页

牌局结束后直接进入结算页，必须展示：

- 四家最终分数、净得分和排名；
- 按时间顺序排列的胡牌、杠分和终局赔付明细；
- 用户本局胡牌次数、点炮次数等基础统计；杠牌统计分别展示明杠、补杠、暗杠次数及各类型对应收支；
- “再来一局”和“返回首页”操作；
- 复盘入口显示“功能建设中”，不输出任何未经验证的复盘建议。

## 9. 关键数据对象

最终 MVP 至少需要：

- 用户及手机号验证码登录状态；
- 用户能力画像及样本量、可信度；
- 牌局与可续局状态；
- 完整牌局事件；
- 决策点及当时可见信息；
- 合法候选动作与评估；
- 复盘报告；
- 主要问题、优秀决策及用户认可/复核反馈；
- 问题类型与用户问题实例；
- 专项题模板、参数和生成题；
- 训练记录；
- 改善验证记录；
- AI 风格、难度状态及每 3 局调整记录；
- 规则版本与评估引擎版本。

完整事件需足以还原牌局、复核规则和支持未来回放；里程碑 1 保存完整事件但不提供可视化回放页，可视化回放不属于正式 MVP。

## 10. 非功能需求

### 10.1 性能与体验

- 常规操作即时反馈；
- AI 普通操作拟人停顿约 0.5–1 秒且不阻塞界面；用户或 AI 胡牌时约停顿 1–2 秒展示胡牌和分数变化；
- 单局目标 8–12 分钟；
- 局后计算复盘时展示进度，复盘目标 3 分钟内完成；
- 移动端首屏和牌桌交互保持流畅。

### 10.2 可解释性

- 每条重要诊断可追溯到具体决策点和当时可见信息；
- 推荐动作展示至少一个当前算法实际使用的关键依据；
- 不允许只输出“AI 认为更好”等不可验证结论；
- 明确区分最终期望得分目标、首版启发式近似和后续模拟评估。

### 10.3 一致性

- 对局引擎、AI、复盘和训练使用同一规则定义；
- 相同输入和评估版本得到可复现结果；
- 规则和评估算法升级记录版本；
- 番型、番数与封顶、响应与优先级、杠分、结束条件、终局顺序及呼叫转移必须有覆盖典型与边界场景的自动化规则验收用例；
- 完整事件回放结果与原牌局一致，存档恢复前后状态一致，任何阶段不得出现非法状态。

### 10.4 公平性、隐私与安全

- AI 和诊断只使用对应决策时点可见的信息；
- 动态难度不得读取隐藏牌或故意送炮；
- 用户身份凭据和验证码流程安全实现；
- 用户只能访问自己的牌局和成长数据；
- 服务端校验关键牌局操作和数据归属；
- 提供账户数据删除能力。

## 11. 最终 MVP 页面清单

1. 手机号验证码登录页；
2. 极短首次使用说明；
3. 首页/今日教练建议/继续牌局；
4. 四方实战牌桌；
5. 牌局结算；
6. 复盘计算进度；
7. 整局复盘总结；
8. 2 个主要问题详情；
9. 1 个优秀决策详情；
10. 动态专项训练及逐题反馈；
11. 训练结果；
12. 验证实战引导；
13. 成长主页；
14. 问题详情；
15. 历史牌局与复盘；
16. 账户设置。

## 12. 实施拆分与优先级

### 12.1 分阶段原则

最终 MVP 保留完整“账户—实战—复盘—训练—验证—成长”目标，但按以下里程碑逐步交付。P0 表示最终 MVP 上线前必须具备，不表示里程碑 1 立即完成所有能力。

### 12.2 里程碑 1：完整对局

**最高优先级：验证规则正确性。**

#### 交付范围

- 重做首页，以“开始实战”为主入口；有未完成存档时提供“继续牌局”，原有训练统一归入专项训练；
- 开始实战后不做开局配置，直接按第 5.1 节完成随机庄家、随机发牌和初始计分；
- 实现第 5 章完整对局、番型、响应、杠分、呼叫转移和终局结算规则；
- 实现系统推荐且用户可修改的定缺；推荐综合花色张数、孤张结构和有效搭子，选择预期牌效损失最小的缺门；实现缺门牌优先打出、持有缺门牌不能胡的限制；
- 实现四方牌桌、独立牌河、手牌排序、摸牌暂置最右、最后弃牌高亮及持续牌桌信息；
- 实现两次点击确认出牌、15 秒出牌倒计时及合法的超时出牌；超时且受定缺约束时，在合法缺门牌中选择牌效损失最小的牌，并尽量不破坏对子和搭子；首版不做自动胡和整局托管；
- 实现全部合法响应按钮、8 秒响应倒计时、超时过、胡 > 杠 > 碰优先级和一炮多响；禁止吃牌；
- 实现多杠项的具体牌与杠类型选择，以及仅补杠可被抢杠胡的规则；
- 实现三种 AI 风格、基本牌效、公平信息边界和拟人停顿；
- 实现血战胡后公开手牌和牌型、退出后续牌局行为与后续款项，并在胡牌后短暂展示分数变化；
- 实现牌墙摸完或三家已胡时结束，并提供第 8.10 节结算页；
- 每个完整牌局事件后立即保存本地存档；暂停场景停止倒计时，支持从最近稳定决策点恢复；恢复后当前操作阶段倒计时重置为完整时限；只保留当前未完成牌局；
- 放弃牌局二次确认后删除存档，该局不进入能力或问题统计；
- 保存完整牌局事件，为规则复核和未来回放保留数据，但不提供可视化回放页；
- 正式验收覆盖手机竖屏和桌面宽屏。

#### 验收标准

- 从“开始实战”可直接进入使用 108 张牌的随机牌局，庄家与手牌数量、起始分数正确；
- 用户和 AI 的定缺、摸打、响应、碰杠胡、血战胡后退出、海底及结束条件在典型与边界场景中均符合第 5 章；定缺推荐综合花色张数、孤张结构和有效搭子，并选择预期牌效损失最小的缺门；
- 番型与番数叠加、5 番封顶、过手胡、抢杠胡、杠分、呼叫转移、一炮多响和终局结算均有自动化测试覆盖典型与边界场景；
- 任意状态下不生成非法动作，不出现重复摸牌、重复结算、已胡玩家继续行动或分数收支不一致；
- 完整事件可确定性还原相同牌局结果，事件顺序、动作、分数和最终结算一致；
- 每个完整事件后的存档均可恢复到最近稳定决策点，恢复前后局面、行动方、随机结果和最终结算一致；恢复后当前操作阶段倒计时重置为该阶段完整时限；
- 出牌与响应倒计时、切后台/关闭页面/暂停菜单停止计时符合已确认口径；超时且受定缺约束时，系统在合法缺门牌中选择牌效损失最小且尽量不破坏对子和搭子的牌；
- 结算页信息完整且明细可与事件记录和最终分数逐项核对；明杠、补杠、暗杠次数及各类型对应收支分别展示；复盘入口只显示“功能建设中”；
- 手机竖屏与桌面宽屏下牌面、牌河、操作按钮、倒计时、分数和当前行动者清晰可用。

#### 明确不包含

里程碑 1 不包含正式手机号账户、云同步、正式复盘分析、动态专项训练、可视化回放、自动胡、整局托管和付费能力。正式 MVP 后续仍采用手机号验证码登录。

### 12.3 里程碑 2：基础复盘

**目标：用可解释启发式评估验证诊断认可度。**

- 局后计算并展示复盘进度；
- 用牌效、危险度、局势规则等启发式近似评估；
- 输出 2 个主要问题和 1 个优秀决策；
- 支持用户提交“不认可”及原因，并标记待复核；
- 不宣称首版结果为精确期望值；
- 建立长期问题聚合、优先级和基础能力评分。

### 12.4 里程碑 3：训练闭环

**目标：形成可验证的产品闭环。**

- 通过模板参数化生成合法相似题；
- 实现逐题即时反馈；
- 打通训练、验证实战和改善状态；
- 实现“今日教练建议”、0–100 能力雷达、样本量和可信度；
- 接入手机号验证码登录、云端同步和跨设备续局，达到正式 MVP 账户要求；
- 验证长期问题阈值、训练达标阈值和明显改善比例。

### 12.5 里程碑 4：算法升级

**目标：从启发式近似逐步接近长期期望得分最大化。**

- 从启发式升级为混合评估和模拟期望值；
- 使用当时可见信息进行长期期望得分估算；
- 继续以可解释性、稳定性和用户认可度校验升级效果；
- 保留算法版本，避免将新旧口径直接混合比较。

### 12.6 最终 MVP P0 验收范围

在上述里程碑完成后，最终 MVP 的 P0 范围包括：

- 手机号验证码账户与跨设备数据；
- 完整血战到底规则及规则测试；
- 三个 AI 风格、公平信息边界和每 3 局动态难度；
- 完整牌局事件记录、本地/云端自动保存和续局；
- 牌效、攻防、鸣牌基础评估；
- 局后总结、2 个主要问题和 1 个优秀决策；
- 复盘认可/不认可与待复核标记；
- 问题画像和跨局聚合；
- 参数化动态专项题和逐题即时反馈；
- 训练后再战验证；
- 今日教练建议、问题清单、能力雷达和成长曲线；
- 闭环行为数据统计。

精确期望值引擎不是首期交付前提；最终评估目标保持不变，并由里程碑 4 的混合评估/模拟逐步逼近。

### 12.7 MVP 后增强

- 关键手互动式追问；
- 更细的能力子维度；
- 可视化牌局回放；
- 复盘分享；
- 多套四川麻将规则；
- 牌谱导入；
- 真人对局；
- 后台运营和诊断纠错工具（是否纳入近期版本待确认）。

## 13. 总体验收场景

一个新用户能够：

1. 使用手机号验证码登录，通过极短说明直接开始首战；
2. 在四方牌桌完成一局符合第 5 章规则的血战到底；
3. 中途退出后从同一决策点续局；
4. 在牌局中不受系统建议干扰，并被明确告知动态难度不读取隐藏信息；
5. 局后看到计算进度，并在目标时间内查看牌效、攻防、鸣牌总结；
6. 查看 2 个主要问题和 1 个优秀决策，并理解当前评估口径；
7. 对结论提交认可或带原因的不认可反馈；
8. 完成系统针对问题生成的合法相似专项题，并逐题获得即时反馈；
9. 达标后开始验证实战；
10. 在训练后至少积累 5 个同类实战决策，并仅在平均损失与错误率均明显下降时看到“已改善”；
11. 从“今日教练建议”继续最合适的下一步；
12. 在其他设备登录后继续查看数据和进度。

上述路径全部可用，才视为最终 MVP 核心闭环完成；各里程碑按其自身目标单独验收。

## 14. 风险与待验证假设

### 14.1 主要风险

- 四川麻将局面分支多，模拟期望值的精度、性能和成本需要平衡；
- 攻防和鸣牌结论可能受风格与局势目标影响，需要避免过度绝对化；
- 动态训练题必须保持局面合法且具有教学意义；
- 单局可能无法覆盖训练问题，因此改善验证需要多局样本；
- 动态难度若只追求胜率平衡，可能损害用户对 AI 公平性的信任；
- 手机号验证码和云端同步增加正式 MVP 的工程与合规成本；
- 单局和复盘时长目标需要在真实设备及目标用户中验证。

### 14.2 待验证事项

以下精确参数保持待验证，不自行预设：

1. 长期问题成立所需的累计决策损失阈值；
2. 训练题组的达标阈值；
3. 训练后平均决策损失和错误率“明显下降”的具体比例；
4. “再次出现”的损失与错误率阈值；
5. 启发式评估与高手判断、用户认可度的目标水平；
6. 模拟期望值的精度、耗时和服务成本上限；
7. 能力雷达可信度分档及样本窗口；
8. 动态难度各档参数、胜负/净得分/偏好的校准权重；
9. 单局 8–12 分钟和复盘 3 分钟内是否适合目标用户；
10. 是否需要在 MVP 后近期增加后台运营和诊断纠错工具。
